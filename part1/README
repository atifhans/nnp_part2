This folder contains code for Part2 of the project1. The simulation and synthesis can be run using the Makefile provided. Below are the targets of the Makefile and their description.

- "make simp" : Use the testbench (mvm3_part1_simple_tb.sv) written by professor milder to compile and simulate the DUT.

- "make sim" : Use the testbench (mvm3_part1_tb.sv) written by me to compile and simulate the DUT.

- "make synth" : Synthesize the DUT.

- "make all" : Run both "sim" and "synth" targets.
